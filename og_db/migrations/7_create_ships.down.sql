
-- Drop the rapide fire against defenses table.
DROP TABLE ships_rapid_fire_defenses;

-- Drop the rapid fire table.
DROP TABLE ships_rapid_fire;

-- Drop the deployment cost table.
DROP TABLE ships_deployment_cost;

-- Drop the propulsion cost table.
DROP TABLE ships_propulsion_cost;

-- Drop the propulsion increase table.
DROP TABLE ships_propulsion_increase;

-- Drop the ships' propulsion table.
DROP TABLE ships_propulsion;

-- Drop the ships' costs table.
DROP TABLE ships_costs;

-- Drop the ships table.
DROP TABLE ships;
